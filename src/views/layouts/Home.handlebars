<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos en Tiempo Real</title>    
    <link href="/css/styles.css" rel="stylesheet" type="text/css" />
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body>
    <header>
        <nav class="navbar">
            <ul class="nav-menu">
                <li><a href="/" class="logo">The Store</a></li>
                <li><a href="/">Inicio</a></li>
                <li><a href="/realtimeproducts">Agregar Producto</a></li>
            </ul>
        </nav>
    </header>
    
    <main>
        <h1>Productos</h1>       

        <div id="product-list" class="product-list">
            {{#each products}}
                <div class="product-card">
                    <img src="{{this.image}}" alt="Imagen de {{this.name}}" class="product-image">
                    <h2>{{this.name}}</h2>
                    <p><strong>ID:</strong> {{this.id}}</p>
                    <p><strong>Descripción:</strong> {{this.description}}</p>
                    <p><strong>Categoría:</strong> {{this.category}}</p>
                    <p><strong>Cantidad:</strong> {{this.quantity}}</p>
                    <div class="product-actions">
                        <a href="/realtimeproducts?id={{this.id}}" class="button-link">Editar</a>
                        <button onclick="deleteProduct({{this.id}})">Eliminar</button>
                    </div>
                </div>
            {{/each}}
        </div>
    </main>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/home.js"></script>
</body>
</html>
